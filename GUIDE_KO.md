# 제국의 투쟁 — 플레이하면서 개발 배우기

이 문서는 게임의 판단 순서와 그 판단을 코드에 옮기는 방법을 함께 설명한다. 규칙의 최종 근거는 GMT의 공식 2쇄 규칙과 카드 텍스트다. 이 문서는 룰북을 그대로 옮긴 자료가 아니다.

## 1. 무엇을 겨루는 게임인가

영국과 프랑스는 지도를 전부 점령하는 대신, 제한된 투자 기회로 필요한 지역과 상품에서 우위를 만든다. 정치 공간은 외교와 전쟁에, 시장은 상품 수요에, 요새와 함대는 연결과 보호 및 전력에 영향을 준다. 한 공간이 여러 목적에 쓰이므로 지금의 점수와 다음 전쟁을 함께 보아야 한다.

승점은 하나의 트랙을 공유한다. 시작은 15이며 프랑스가 득점하면 숫자가 올라가고 영국이 득점하면 내려간다. 따라서 영국의 좋은 행동 뒤 숫자가 작아지는 것은 정상이다. 자동 승리는 규칙에서 정한 확인 시점에 판정한다.

| 평화 턴 | 시대 | 턴 뒤의 전쟁 |
|---|---|---|
| 1 | 왕위 계승 | 없음 |
| 2 | 왕위 계승 | 스페인 왕위 계승 전쟁 |
| 3 | 제국 | 오스트리아 왕위 계승 전쟁 |
| 4 | 제국 | 7년 전쟁 |
| 5 | 혁명 | 미국 독립 전쟁 |
| 6 | 혁명 | 최종 득점 |

## 2. 첫 행동 라운드의 순서

1. 이번 세계 수요와 지역 보상을 읽는다. 시장을 늘리려는 이유가 수요인지, 전쟁을 준비하려는 것인지 먼저 정한다.
2. 사용 가능한 투자 타일을 비교한다. 큰 숫자는 주요 행동, 작은 숫자는 보조 행동이다. 이벤트와 전쟁 준비 기호도 확인한다.
3. 타일을 선택하고 이벤트를 사용하거나 건너뛴다. 이벤트는 다른 요소를 사용하기 전에 처리한다.
4. 이벤트가 요구하는 대상과 선택지를 완료한다. 다른 행동점수를 먼저 쓰려고 해도 선택이 대기 중이면 진행하지 않는다.
5. 주요 행동, 보조 행동, 별도 이벤트 행동, 전쟁 준비의 순서를 정한다. 시작한 행동을 떠나면 돌아갈 수 없는 경우가 있으므로 화면의 비활성 버튼을 확인한다.
6. 할 일을 마쳤으면 행동 라운드 종료를 누른다.

타일의 어느 요소도 쓰지 않았다면 패스로 부채를 줄일 수 있다. 이벤트나 전쟁 준비를 사용한 뒤 패스 보상을 받는 방식은 허용되지 않는다.

## 3. 행동점수는 숫자와 출처를 함께 가진다

경제 EP, 외교 DP, 군사 MP는 서로 다른 자원이다. 같은 종류라도 투자 타일에서 받은 점수와 카드에서 받은 점수에는 다른 제한이 있을 수 있다. 이 때문에 내부 상태에 단순한 `남은 AP = 5`만 저장해서는 안 된다.

예를 들어 주요 외교 3점과 인도 전용 이벤트 외교 2점을 받았다면 전체 숫자는 5다. 그러나 유럽 행동에 5점을 모두 쓸 수는 없다. 이벤트 점수를 별도 행동으로 사용할지, 규칙이 허용하는 주요·보조 행동에 합산할지도 먼저 정해야 한다. 합산해도 카드의 대상 제한은 남는다.

구현에서는 각 이벤트 점수를 `event_grants`에 담는다. 각 항목은 종류, 잔량, 귀속 행동, 제한을 가진다. `ap_for_current(target, kind)`는 특정 대상과 행동에 실제로 쓸 수 있는 점수만 합한다. `can_spend_ap`는 조회하고, `spend_ap`가 실제 잔량을 바꾼다.

보조 행동은 지출 횟수에도 제한이 있다. 2점 중 1점을 썼다고 남은 1점으로 두 번째 공간을 자유롭게 바꿀 수 있는 것이 아니다. 주요 행동과 숫자만 다르게 구현하면 이 규칙을 놓친다.

6턴에는 군사 2점을 경제 또는 외교 1점으로 변환할 수 있다. 이번 라운드에 정한 변환 종류를 유지해야 하며, 지역이나 용도 제한을 변환으로 우회할 수 없다. 전쟁 타일 전용 점수는 일반 경제 점수로 바꿀 수 없다.

## 4. 비용을 계산하는 올바른 순서

### 외교 점수로 카드를 뽑는 예

보조 외교 2점을 선택한 상태에서 조약점수 1점을 보충하면 외교 3점이 된다. 이벤트 카드 뽑기를 누르면 3점을 한 번에 쓰고 카드 1장을 손패에 더한다. 이 지출로 보조 행동을 사용했으므로 추가 점수를 얻더라도 같은 보조 행동에서 또 지출하지는 못한다. 주요 외교는 그 제한이 없어 6점이면 두 장을 구입할 수 있다.

합산한 점수의 총량과 특정 행동에 쓸 수 있는 점수는 다르다. 주요 외교 2점과 유럽 전용 외교 3점이 있으면 화면에는 총 5점이 표시된다. 하지만 카드 뽑기는 유럽 공간에 하는 행동이 아니므로 사용 가능한 점수는 2점이다. 이때 일반 채무로 1점을 보충하면 일반 점수 3점만 지출하고 유럽 전용 3점은 남긴다.

코드에서는 `draw_event_block_reason`이 불가 이유를 반환하고, `draw_event_card`가 실제 지출과 카드 획득을 처리한다. UI와 AI는 이 메서드를 함께 호출한다. 카드 분배 때 사용하는 `_draw_event`에 재혼합 허용 여부를 명시하여, 외교 행동으로 버림 더미를 마음대로 다시 섞지 않게 했다.

무작위 정보는 실행 취소의 경계가 된다. 채무를 늘리고 카드를 본 뒤 채무 증가까지 되돌리면 이미 본 카드를 다시 더미에 넣을 수 있기 때문이다. 따라서 카드 획득 후에는 과거 실행 취소 기록을 지우고, 손패·더미 순서·남은 점수·보조 행동 사용 여부를 기존 저장 그래프에 보존한다. 손패가 4장이 되어도 즉시 버리지 않으며 다음 카드 분배 단계에서 3장으로 정리한다. 실제 화면은 가로 스크롤로 추가 카드를 담는다.

### 기본 비용과 보정

공간에 인쇄된 기본 비용에 현재 상태의 증감과 할인을 모두 반영한 뒤 최솟값을 적용한다. 중간에 먼저 최소 1로 올리면 이후 할인에서 잘못된 결과가 나온다. 구현의 `calculate_shift_cost` 주석에는 분쟁, 보호, 고립, 지역 변경, 내각·이점 할인 순서의 이유가 있다.

가령 기본 비용이 2이고 어떤 합법적인 상황에서 총 감소가 2, 증가가 1이라면 최종 계산은 `max(1, 2 - 2 + 1) = 1`이다. 감소를 적용한 직후 `max(1, 0)`으로 바꾼 다음 증가를 더하면 2가 되어 잘못된 비용을 청구한다. 이 숫자 예시는 계산 순서를 설명하며 특정 공간의 모든 조건을 가정한 예시는 아니다.

공간 위에 마우스를 올리면 현재 판에서 필요한 비용이나 불가능한 이유가 나온다. 연결되지 않은 시장, 온전한 상대 요새, 아직 등장하지 않은 시대의 공간은 점수가 충분하다는 이유만으로 바꿀 수 없다.

## 5. 지도는 그림과 연결 그래프로 나뉜다

지도 이미지는 사람에게 위치와 관계를 보여 준다. 규칙 엔진은 공간 ID와 연결 목록을 사용한다. 그림에서 가까운 공간이라고 코드에서도 연결된 것은 아니며, 서로 다른 종류의 선을 하나로 합치면 안 된다.

| 연결 데이터 | 사용하는 판단 |
|---|---|
| 실선 연결 | 시장 접근, 고립, 요새·함대와의 관계 |
| 정복선 | 전쟁에서 해당 영토를 정복할 수 있는 근거 |
| 이점 연결 | 어느 공간을 확보하면 이점을 지배하는지 |
| 지역·하위 지역 | 전력, 지역 보상, 카드 대상 제한 |

예를 들어 Madras의 정복 근거는 Arcot 또는 Vandavasi 요새와의 정복선이다. 단순히 인도에 깃발이 있다는 이유로 정복을 허용하지 않는다. Halifax의 하위 지역도 이름의 인상으로 추정하지 않고 원작 지도 경계를 따른다.

`data/build_verified_map.py`와 `data/conquest_lines.json`에 검증한 관계가 있다. 정복선 병합 시 영토만 초기화하면 요새 쪽에 오래된 역방향 선이 남으므로 모든 공간의 기존 정복선을 비우고 다시 만든다.

## 6. 카드 효과를 한 번에 실행하지 않는 이유

카드는 단순한 수치 증가만 요구하지 않는다. 플레이어가 공간을 고르고, 상대가 손패를 버리고, 마지막 결과를 보고 조건부 득점하는 순서가 생긴다. 이런 효과를 긴 함수 하나에서 자동 선택하면 사람의 결정을 잃고 저장도 어려워진다.

`EventEffects.pending_choices`는 아직 끝나지 않은 효과의 순서다. 지도 대상은 보드에서, 비공간 선택은 `CardChoices`와 선택 창에서 처리한다. 선택을 완료하면 다음 효과로 넘어간다.

여기에는 두 종류의 조건이 있다. 이벤트 보너스를 사용할 자격은 기본 효과 전에 판단한다. 반면 카드가 기본 효과의 결과를 조건으로 득점하라고 한다면 그 득점은 기본 효과 뒤에 계산한다. Vatican Politics처럼 기본 효과가 마지막 상대 깃발을 제거할 수 있는 카드에서 이 차이가 드러난다.

비공개 내각도 주의한다. 비용을 미리 보여 주려고 조회했을 뿐인데 내각이 공개되면 정보가 새어 나간다. 가능 여부를 읽는 함수와 실제 사용·공개하는 함수를 구분한 이유다.

사용자 피드백을 반영하여 공개 선택을 더 엄격히 구분했다. `MinistryDecisions`는 관련 행동을 잠시 보류하고 공개 여부를 묻는다. 공개를 승인하면 효과를 포함해 원래 행동을 재개하고, 거절하면 그 내각 없이 다시 합법성과 비용을 확인한다. 명령과 대상은 데이터로 저장하므로 질문이 열린 중간 상태도 복원할 수 있다.

공개는 자기 행동 라운드에만 가능하다 (§3.5). 득점이나 전쟁 계산이 비공개 내각을 발견했다고 자동으로 공개해서는 안 된다. 그때의 효과를 원하면 앞선 자기 라운드에서 미리 공개한다. 공개된 뒤에 적용되는 효과와, 처음부터 공개를 선택할 수 있는 시점은 별개의 규칙이다.

또한 ‘라운드의 첫 번째 이점 사용’이라는 발동 기회는 효과의 소진 여부와 다르다. 첫 기회에 공개를 거절했다면 카드는 소진되지 않아도 그 기회는 지나간다. 이를 `active_flags`의 기회 기록으로 구분했다. 북미 무역의 시장 우위 조건은 라운드 시작에 저장하여, 나중에 공개하면서 현재의 시장 수로 소급 판정하지 않는다.

## 7. 전쟁은 전장별로 끝까지 처리한다

전쟁을 시작하면 모든 전장의 결과를 한꺼번에 확정하지 않는다. 현재 전장에서 타일 효과를 처리하고 전력을 계산한 다음, 전리품과 정복 선택까지 마친 뒤 다음 전장으로 간다. 앞 전장의 변화가 뒤 전장의 조건에 영향을 줄 수 있기 때문이다.

`WarFlow`는 현재 단계 `stage`, 전장 번호 `index`, 처리할 작업 `operations`, 기다리는 선택 `choice`를 가진다.

```text
준비 → 타일 효과 → 전력 계산 → 전리품·정복 선택 → 전장 종료
  ↑                                                     │
  └──────────────── 다음 전장 ───────────────────────────┘
```

정복점수로 영토를 요구하면 상대에게 양도 거부 선택이 생길 수 있다. 그동안 전쟁은 `choice`를 보유한 채 멈춘다. 사용자가 저장하면 그 선택도 함께 저장된다. 다시 불러왔을 때 앞 전리품을 중복 지급하거나 다른 전장으로 건너뛰어서는 안 된다.

기본 타일 교체와 보너스 타일 배치도 비슷하다. 이미 뽑아 본 타일을 취소하고 다시 뽑으면 정보에 따라 난수를 고르는 효과가 생긴다. 따라서 뽑은 타일은 화면의 임시 변수가 아니라 저장되는 모델에 두며, 결정을 끝내기 전 다른 행동을 막는다.

## 8. 저장은 객체의 관계도 보존해야 한다

선택된 내각과 전체 내각 목록이 같은 카드를 가리킬 수 있다. 각각을 별도 복사로 복원하면 한쪽에서 공개해도 다른 쪽은 비공개로 남는다. 이런 문제를 막기 위해 `SessionCodec`은 객체를 ID로 관리하고 참조 관계를 보존한다.

`SaveLoad`는 게임 상태, 행동점수, 카드 대기, 전쟁 진행, 이점과 내각 상태, AI 진영, 행동 기록을 하나의 그래프로 직렬화한다. 등록된 모델만 복원하며, 먼저 전체 데이터를 해석·검사한 다음 현재 판에 적용한다. 화면 노드나 버튼, 함수 호출 자체를 저장하지 않는다.

실행 취소는 같은 저장 구조의 스냅샷을 사용한다. 스냅샷 안에 이전 실행 취소 기록 전체를 다시 넣으면 크기가 계속 커지므로, 새 스냅샷을 만들 때 기존 실행 취소 스택을 잠깐 제외한다. 무작위 정보를 확인하는 행동 등은 실행 취소 경계를 만든다.

## 9. 코드를 읽는 순서

| 순서 | 파일 | 읽을 때 볼 것 |
|---|---|---|
| 1 | `scripts/models/game_state.gd` | 진영, 시대, 턴, 승점의 의미 |
| 2 | `scripts/autoload/game_manager.gd` | 턴의 단계와 사람 선택 요청 |
| 3 | `scripts/autoload/action_controller.gd` | 합법성 조회와 실제 상태 변경의 분리 |
| 4 | `scripts/autoload/event_effects.gd`, `card_choices.gd` | 카드별 효과와 선택 대기 |
| 5 | `scripts/autoload/ministry_effects_v2.gd`, `advantage_manager.gd` | 지속 효과, 수동 사용, 소진 |
| 6 | `scripts/autoload/war_flow.gd` | 저장 가능한 전쟁 단계 |
| 7 | `scripts/models/session_codec.gd`, `autoload/save_load_v2.gd` | 참조 보존과 안전한 복원 |
| 8 | `scripts/game_session.gd`, `scripts/ui/action_panel_v2.gd` | 모델 신호를 화면으로 연결 |

표의 경로는 모두 `godot_project` 아래다. 프로젝트에 예전 파일도 남아 있다. 실제로 실행되는 스크립트는 `project.godot`의 Autoload와 각 `.tscn`의 참조를 먼저 확인한다. 예를 들어 저장은 `save_load_v2.gd`, 전쟁 진행은 `WarFlow`, 행동 패널은 `action_panel_v2.gd`가 담당한다.

## 10. 기능을 바꿀 때의 연습 절차

1. PRD에 바꾸려는 동작과 완료 기준을 적는다. 공식 카드나 룰북의 근거도 함께 남긴다.
2. 정상 예와 거절해야 할 예를 각각 하나 만든다. 예를 들어 인도 전용 AP로 인도 행동은 가능하고 유럽 행동은 불가능해야 한다.
3. 모델에서 판정과 상태 변경을 먼저 구현한다. UI에서만 버튼을 막으면 AI나 불러오기 경로로 규칙을 우회할 수 있다.
4. 필요한 선택을 데이터로 표현한다. 그 중간 상태를 저장하고 불러와도 같은 결정을 할 수 있는지 확인한다.
5. 한국어·영어 설명과 버튼을 연결한다. 글꼴의 한글 지원과 양쪽 언어의 긴 문장 줄바꿈을 실제 화면에서 확인한다.
6. 수정에 관련된 검사부터 실행한다. 전체 흐름을 바꿨다면 캠페인과 UI 완주도 실행한다.
7. claude.md에 한 일, 검증 결과, 남은 판단을 갱신한다.

기존 97개 카드 검사는 41개 카드의 양 진영 효과가 선택을 마칠 수 있는지 보는 82개와 별도 경계 검사 15개로 구성된다. 모든 가능한 조합의 정확한 숫자를 97개 모두가 검사한다는 뜻은 아니다. 세부 규칙은 별도의 수치·거절 검사를 추가하여 보완한다.

## 11. 화면 배치를 읽는 방법

`hud_dashboard.gd`는 자원과 보상을 표시하고, `session_layout.gd`는 지도와 작업 영역의 크기를 공유한다. 지도 이미지의 비율과 클릭 좌표 변환에 같은 크기를 사용해야 화면만 줄였을 때 깃발을 누르는 위치가 어긋나는 문제를 막을 수 있다.

투자 선택 전에는 타일 비교가 중요하고 선택 후에는 행동이 중요하다. `investment_display.gd`는 선택 후 타일 목록을 접고, 펼침 상태가 바뀔 때 `game_session.gd`가 투자 목록과 행동 패널을 함께 배치한다. 버튼의 색만으로 상태를 알리지 않고 ‘선택 중’, ‘남은 3점’, ‘완료’라는 문구도 표시한다.

세계 수요는 기본 3종이지만 이벤트로 늘어날 수 있다. 상품 수에 맞춰 칸 너비를 나누고 모든 수요를 표시한다. 화면 보상과 실제 득점은 `GameManager.commodity_reward`라는 같은 표를 읽는다. 같은 숫자를 두 파일에 따로 적어 두면 시대별 보상이 수정될 때 한쪽만 바뀔 위험이 있기 때문이다.

해군 상자는 `navy_box_view.gd` 하나를 중앙 정보창과 지도에서 함께 사용한다. 공통으로 `squadrons_in_navy_box`를 읽고, `squadron_strip.gd`가 한 척당 원작 말 하나를 그린다. `squadrons_on_map`은 이미 지도에 배치된 수이고, `squadrons_returning`은 나중에 돌아올 수이므로 대기 말에 더하지 않는다. 미건조 수는 영구 제거를 뺀 건조 한도에서 현재 전체 함대 수를 빼서 구한다.

현재 모델에는 함대 수 전용 변경 신호가 없다. 그래서 해군 표시는 몇 개의 숫자와 현재 언어를 비교하고, 달라질 때만 문구와 그림을 다시 그린다. 건조·배치·전쟁·귀환·저장 복원이 서로 다른 경로에서 일어나도 표시가 오래된 숫자에 머무르지 않게 하기 위한 선택이다. 게임 규칙이나 모델 값은 화면에서 수정하지 않는다.

지도 해군 표시는 `BoardLayer`의 자식이다. 부모가 확대·이동하면 같은 변환을 물려받아 원작 해군 상자 위치를 유지한다. 표시 노드는 마우스 입력을 무시하므로 지도 드래그나 공간 선택을 가로채지 않는다. 작은 지도 글자는 더 큰 글리프로 만든 뒤 축소하여 확대할 때 한글 획이 흐려지는 것을 줄인다.

`reward_icon.gd`는 우승 잔·문서·동전을 도형으로 그린다. 이모지를 쓰면 PC 글꼴에 따라 네모로 깨질 수 있으므로 글꼴과 독립된 그림으로 만들었다. 상품은 기존 원작 이미지를 사용하고, 모든 아이콘 옆에는 전체 이름·값을 남긴다. 채무 보상의 부호를 그림으로 대체하지 않는 이유는 +1이 빚 증가라는 규칙상 의미를 분명히 유지하기 위해서다.

## 12. 온라인 대전을 나중에 붙이는 방법

현재 게임은 같은 컴퓨터의 모델을 UI와 AI가 직접 호출한다. 온라인은 아직 구현되지 않았다. 다음 단계에서는 `깃발 변경 요청`, `이벤트 대상 선택`, `정복 양도 응답` 같은 명령을 별도 데이터로 정의하는 것이 출발점이다.

권한을 가진 하나의 게임 상태가 명령의 진영, 차례, 비용을 검증한 뒤 변경을 확정해야 한다. 각 참가자에게는 자신에게 허용된 정보만 전달해야 한다. 로컬 교대용 가림 화면은 온라인의 정보 보안 장치가 될 수 없다.

저장 가능한 선택 상태와 UI에서 분리된 합법성 검사는 이 확장에 도움이 된다. 다만 현재 구조가 곧바로 네트워크 대응 완료라는 뜻은 아니다. 접속 방식, 난수 권한, 재접속, 명령 중복 방지와 버전 호환을 후속 PRD에서 결정해야 한다.

## 13. 언어는 게임 상태와 분리한다

같은 카드가 한국어에서는 ‘존 로’, 영어에서는 ‘John Law’로 보이더라도 게임 안에서는 같은 카드여야 한다. 규칙의 ID나 저장값을 번역하면 언어를 바꾸는 순간 다른 카드로 취급하는 오류가 생길 수 있다. 그래서 `LocaleManager.local_name`은 모델에 있는 영어·한국어 표시 필드 중 하나만 선택하고, 모델 자체를 수정하지 않는다.

새 화면 문구는 `LocaleManager.tx`와 `locale/ui_en.json`으로 관리한다. 한국어 문장 전체를 키로 쓰므로 코드를 읽을 때 의미를 알 수 있다. ‘남은 %d점’처럼 숫자가 들어가는 문장은 번역에도 같은 서식 인자를 유지한다. 번역 누락은 오류로 기록하고, 회귀 검사가 전체 문구의 번역과 인자 수·종류를 확인한다.

기존 저장 기록과 규칙 모델의 안내에는 이미 숫자와 이름이 들어간 한국어 문장이 있다. `LocaleManager.message`는 그 문장을 화면에 표시할 때 원래 문장 형식과 대조하고, 인자 속 지명·카드명도 영어 원문으로 바꾼다. 과거 저장 기록을 덮어쓰지 않기 때문에 한국어로 돌아가면 원래 기록을 그대로 읽을 수 있다.

언어 전환 신호를 받으면 정적 버튼과 동적 화면을 갱신한다. 이때 화면을 새로 그린다는 이유로 고른 내각이나 이벤트 선택을 초기화하면 안 된다. 선택은 유지한 채 문구만 갱신하고, 언어 설정은 `settings.cfg`에 따로 저장한다. 검사는 전환 전후의 게임 저장 그래프를 비교해 상태가 바뀌지 않는지 확인한다.

영어는 같은 뜻이라도 길이가 달라 실제 화면 검수가 필요하다. 보상은 두 줄로 나누고, 긴 손패 요약은 말줄임과 상세 보기를 제공하며, 행동·전쟁 이름은 전체 이름에 줄바꿈을 적용했다. 한국어·영어 UI 완주 외에 실제 렌더링과 버튼 조작을 따로 검사하는 이유다.


## 14. 전략 AI의 계산을 따라가기

전략 AI는 어떤 공간이 좋은가를 평가하는 함수와, 그 공간까지 도달하는 행동 순서를 시험하는 검색을 나눈다. 평가가 좋아도 규칙상 실행 불가능하면 명령이 거부된다. 원본 게임을 복제한 별도 프로세스에서 같은 규칙으로 계산하고, 공개 정보와 자기 정보만 담은 관찰을 이용한다.

[전략 AI 구현·학습 안내](docs/AI_IMPLEMENTATION.md)에 시장의 보상 변화, UCB와 라운드 롤아웃, 비공개 표본, 시간 제한·저장, 소스 읽는 순서를 자세히 정리했다. [검증 기록](docs/VALIDATION.md)에서는 규칙 검사·프라이버시 검사·강도 비교를 구분해 읽을 수 있다.
